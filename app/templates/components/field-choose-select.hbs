<select class="form-control" onchange={{action "changedSelect" value="target.value"}}>
{{#each select.options as |option|}}
	<option value="{{option.value}}">{{option.text}}</option>
{{/each}}
</select>

{{#if optionName}}
	optionName exists<br>
{{/if}}

<div class="children">

	{{!-- {{#if setOption}}
		{{#each select.options as |option|}}
			{{#if (eq option.value setOption)}}
				{{#each option.children as |child|}}
					{{field-choose-select
						selects = selects
						name = child
						filterToggle = filterToggle}}
				{{/each}}
{{!-- 			{{/if}}
		{{/each}}
	{{else}}
		{{#each select.options.0.children as |child|}}
			{{field-choose-select
				selects = selects
				name = child
				filterToggle = filterToggle}}
		{{/each}}
	{{/if}} --}}
</div>