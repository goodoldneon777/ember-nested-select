import Ember from 'ember';

export default Ember.Component.extend({
	foo: 'hi',
	operators: ['=', '>', '>=', '<', '<=', 'Range', '!=']
	// var foo = this.get('fieldChooseFilter');
	// console.log(foo);
	// init: {

	// }
});
