import { exists } from '../../../helpers/exists';
import { module, test } from 'qunit';

module('Unit | Helper | exists');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = exists(42);
  assert.ok(result);
});
