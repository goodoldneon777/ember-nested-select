import { notEq } from '../../../helpers/not-eq';
import { module, test } from 'qunit';

module('Unit | Helper | not eq');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = notEq(42);
  assert.ok(result);
});
