import { getFieldChooseObject } from '../../../helpers/get-field-choose-object';
import { module, test } from 'qunit';

module('Unit | Helper | get field choose object');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getFieldChooseObject(42);
  assert.ok(result);
});
